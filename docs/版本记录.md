# 版本记录

## v0.1.0-mvp

MVP 主流程已跑通，当前可用于演示和后续迭代。

支持能力：

- 项目管理
- 环境配置
- Swagger/OpenAPI 或 curl 导入
- 接口资产管理
- mock AI 用例生成
- 测试用例管理
- Pytest + Requests 执行
- 执行结果和报告查看

核心路径：

```text
AI 模块：backend/app/ai
Pytest 模板：backend/app/generator/templates/pytest_project
生成工程：storage/generated/project_{project_id}/execution_{task_id}/
测试报告：storage/reports/execution_{task_id}/report.html
执行日志：storage/logs/execution_{task_id}/pytest.log
上传文件：storage/uploads
```

## 第 9 阶段更新

第 9 阶段在 `v0.1.0-mvp` 基线上增强接口导入能力：

- 支持在线 OpenAPI/Swagger JSON URL。
- 支持 Knife4j / Swagger UI 页面地址自动发现真实 JSON 文档。
- 支持 `/v3/api-docs`、`/v2/api-docs`、`/swagger-resources`、`/api-docs`、`/openapi.json`、`/swagger.json` 候选地址。
- 支持单接口文档页面 URL 的 hash hint 筛选。
- 支持接口路径、请求方法、名称、分组、关键词筛选。
- 支持 Swagger/OpenAPI JSON 内容。
- 保留 curl 文本导入。
- 支持仅预览、保存选中、保存全部。
- 保存前允许用户编辑确认。
- 未找到已有接口资产时只创建待完善接口草稿。
- 支持基础 `$ref` 展开。
- 支持参数 JSON、Body JSON、响应 JSON 的“展开示例 / 展开 Schema”切换。
- 支持编辑预览 JSON 并保存到接口资产。
- 支持执行报告页多选测试用例执行。
- 支持展开单条执行结果，查看并复制请求参数 JSON、响应体 JSON 和 curl 命令。

## v0.1.0-stage9

第 9 阶段收尾版本，基于 `v0.1.0-mvp` 增强接口导入、Cookie 抓取、JSON 预览编辑和执行报告排查能力。

收尾确认内容：

- 第 9 阶段代码已通过 Git commit 和 tag 固化。
- 数据库项目、接口资产、测试用例、执行记录等数据已通过 `mysqldump` 备份。
- 已执行后端测试、前端类型检查、前端构建、健康检查接口和一次完整测试用例执行。
- 当前仍为 mock AI，未接入真实大模型。
- 当前执行引擎仍为 Pytest + Requests。
- 当前可用于演示，也可以在确认后进入第 10 阶段鉴权配置与业务断言增强。

当前限制：

- AI 仍为 mock。
- 未接真实 AI 大模型。
- 未实现复杂接口依赖编排。
- 未实现复杂 token 自动刷新。
- 未实现 Word/PDF 解析。
- 未实现权限系统、CI/CD、多用户协作和生产级部署。

## 第 10 阶段更新

第 10 阶段在 `v0.1.0-stage9` 基线上增强鉴权配置与业务断言能力：

- 环境配置页支持鉴权类型、Token、Cookie、Header JSON、`auth_config_json`、timeout 和 retry。
- 执行引擎自动合并环境公共 headers、环境鉴权 headers 和测试用例请求 headers。
- 请求头合并优先级为：环境公共 headers < 环境鉴权 headers < 测试用例请求 headers。
- 支持 `business_code`、`business_success`、`json_path_not_empty` 业务断言。
- 响应 JSON 中存在 `code` 或 `success` 且测试用例未显式断言时，自动补充默认业务断言。
- 避免 HTTP 200 但业务失败仍被判定为通过。
- 执行结果保存脱敏后的请求数据、响应体、curl 命令和断言结果。
- 非 JSON 响应会保存原始文本、Content-Type 和 JSON 解析错误提示。
- 执行报告页展示断言结果，并继续支持请求参数、响应体、curl 的展开和复制。
- 当前仍不支持自动 token 刷新、自动登录、验证码、SSO 或浏览器自动化登录。
- 当前仍为 mock AI，未接入真实大模型。

## v0.10.1

第 10.1 阶段在 `v0.10.0` 基线上完成断言系统 2.0 融合引擎：

- 新增 `backend/app/core/assertion_engine_v2.py`。
- 支持统一断言结构：`source`、`type`、`path`、`operator`、`expected`、`priority`、`enabled`。
- 支持 AI 断言建议、Swagger/OpenAPI 基础断言、用户断言三层融合。
- 融合优先级为：用户断言 > Swagger/OpenAPI 断言 > AI 建议断言。
- 同一路径断言去重时保留最高优先级断言。
- `business_code` 不再写死为 1，支持 `success_codes` 和 `success_expression`。
- `$.data` 相关 JSONPath 仅在业务成功后执行。
- mock AI 仅生成带 `confidence` 的结构化断言建议，不决定 pass/fail。
- 执行结果中保留最终融合断言和断言来源。
- 前端支持查看 AI 断言建议、Swagger 断言、用户断言和融合断言预览。
- 向下兼容 `v0.10.0`，不影响接口导入、鉴权配置和 Pytest + Requests 执行主流程。

## v0.10.2

第 10.1 阶段继续增强断言体验，上线断言 DSL 可视化系统：

- DSL 断言系统上线。
- AI / Swagger / 用户断言统一使用 DSL 展示。
- 测试用例编辑页新增可视化断言编辑器。
- 支持新增、删除、编辑、复制 DSL。
- 支持模板、AI 建议、Swagger 断言一键插入。
- 新增 DSL 与内部 JSON 断言结构双向转换接口。
- 执行引擎适配 DSL 输入，执行前转换为内部结构化断言。
- AI mock 断言输出简化为 `assertions: string[]`。
- 不修改数据库结构，不接入真实 AI。

后续路线：

- 第 11 阶段：真实 AI 大模型接入
- 第 12 阶段：接口依赖关系与链路用例
- 第 13 阶段：报告和失败分析增强
- 第 14 阶段：CI/CD、定时任务与权限系统
